<UserControl
    x:Class="TrainBookingApp.Views.Manager.TrainTypeManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TrainBookingApp.Views.Manager">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Train Type List  -->
        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,10"
                FontSize="16"
                FontWeight="Bold"
                Text="Train Type List" />

            <!--  Search  -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    Height="30"
                    Padding="5"
                    VerticalAlignment="Center"
                    Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />

                <Button
                    Grid.Column="1"
                    Width="80"
                    Height="30"
                    Margin="5,0,0,0"
                    Command="{Binding SearchTrainTypesCommand}"
                    Content="Search" />
            </Grid>

            <!--  Train Type DataGrid  -->
            <DataGrid
                Grid.Row="2"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                ItemsSource="{Binding TrainTypes}"
                SelectedItem="{Binding SelectedTrainType}"
                SelectionChanged="TrainTypeDataGrid_SelectionChanged"
                SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="150"
                        Binding="{Binding TypeName}"
                        Header="Type Name" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding AverageVelocity}"
                        Header="Avg. Velocity" />
                    <DataGridTextColumn
                        Width="200"
                        Binding="{Binding Description}"
                        Header="Description" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!--  Train Type Form  -->
        <Grid Grid.Column="1" Margin="15,5,5,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,15"
                FontSize="16"
                FontWeight="Bold"
                Text="Train Type Details" />

            <TextBlock
                Grid.Row="1"
                FontWeight="Bold"
                Text="Type Name:" />
            <TextBox
                Grid.Row="2"
                Height="30"
                Margin="0,5,0,10"
                Text="{Binding NewTypeName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock
                Grid.Row="3"
                FontWeight="Bold"
                Text="Average Velocity (km/h):" />
            <TextBox
                Grid.Row="4"
                Height="30"
                Margin="0,5,0,10"
                Text="{Binding NewAverageVelocity, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock
                Grid.Row="5"
                FontWeight="Bold"
                Text="Description:" />
            <TextBox
                Grid.Row="6"
                Height="80"
                Margin="0,5,0,15"
                AcceptsReturn="True"
                Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />

            <!--  Train Type Action Buttons  -->
            <StackPanel Grid.Row="9" VerticalAlignment="Bottom">
                <Button
                    Height="35"
                    Margin="0,5"
                    Background="LightGreen"
                    Command="{Binding AddTrainTypeCommand}"
                    Content="Add Train Type" />

                <Button
                    Height="35"
                    Margin="0,5"
                    Background="LightBlue"
                    Command="{Binding UpdateTrainTypeCommand}"
                    Content="Update Train Type" />

                <Button
                    Height="35"
                    Margin="0,5"
                    Background="LightCoral"
                    Command="{Binding DeleteTrainTypeCommand}"
                    Content="Delete Train Type" />

                <Button
                    Height="35"
                    Margin="0,5"
                    Command="{Binding ClearFormCommand}"
                    Content="Clear Form" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
