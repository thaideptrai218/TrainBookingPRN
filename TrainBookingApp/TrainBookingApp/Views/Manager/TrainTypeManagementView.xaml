<UserControl x:Class="TrainBookingApp.Views.Manager.TrainTypeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TrainBookingApp.Views.Manager">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Train Type List -->
        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="Train Type List" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            
            <!-- Search -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                         Height="30"
                         Padding="5"
                         VerticalAlignment="Center"
                         Watermark="Search train types..."/>
                
                <Button Grid.Column="1" 
                        Content="Search" 
                        Command="{Binding SearchTrainTypesCommand}"
                        Width="80" 
                        Height="30"
                        Margin="5,0,0,0"/>
            </Grid>
            
            <!-- Train Type DataGrid -->
            <DataGrid Grid.Row="2" 
                      ItemsSource="{Binding TrainTypes}"
                      SelectedItem="{Binding SelectedTrainType}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionChanged="TrainTypeDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Type Name" Binding="{Binding TypeName}" Width="150"/>
                    <DataGridTextColumn Header="Avg. Velocity" Binding="{Binding AverageVelocity}" Width="100"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <!-- Train Type Form -->
        <Grid Grid.Column="1" Margin="15,5,5,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="Train Type Details" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>
            
            <TextBlock Grid.Row="1" Text="Type Name:" FontWeight="Bold"/>
            <TextBox Grid.Row="2" 
                     Text="{Binding NewTypeName, UpdateSourceTrigger=PropertyChanged}"
                     Height="30" 
                     Margin="0,5,0,10"/>
            
            <TextBlock Grid.Row="3" Text="Average Velocity (km/h):" FontWeight="Bold"/>
            <TextBox Grid.Row="4" 
                     Text="{Binding NewAverageVelocity, UpdateSourceTrigger=PropertyChanged}"
                     Height="30" 
                     Margin="0,5,0,10"/>
            
            <TextBlock Grid.Row="5" Text="Description:" FontWeight="Bold"/>
            <TextBox Grid.Row="6" 
                     Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"
                     Height="80" 
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,5,0,15"/>
            
            <!-- Train Type Action Buttons -->
            <StackPanel Grid.Row="9" 
                        VerticalAlignment="Bottom">
                <Button Content="Add Train Type" 
                        Command="{Binding AddTrainTypeCommand}"
                        Height="35" 
                        Margin="0,5"
                        Background="LightGreen"/>
                
                <Button Content="Update Train Type" 
                        Command="{Binding UpdateTrainTypeCommand}"
                        Height="35" 
                        Margin="0,5"
                        Background="LightBlue"/>
                
                <Button Content="Delete Train Type" 
                        Command="{Binding DeleteTrainTypeCommand}"
                        Height="35" 
                        Margin="0,5"
                        Background="LightCoral"/>
                
                <Button Content="Clear Form" 
                        Command="{Binding ClearFormCommand}"
                        Height="35" 
                        Margin="0,5"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>