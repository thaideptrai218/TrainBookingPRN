<UserControl x:Class="TrainBookingApp.Views.Manager.RouteManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TrainBookingApp.Views.Manager">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Route List -->
        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="Routes" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            
            <!-- Search -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                         Height="25"
                         Padding="3"
                         VerticalAlignment="Center"
                         Watermark="Search routes..."/>
                
                <Button Grid.Column="1" 
                        Content="Search" 
                        Command="{Binding SearchRoutesCommand}"
                        Width="60" 
                        Height="25"
                        Margin="5,0,0,0"/>
            </Grid>
            
            <DataGrid Grid.Row="2" 
                      ItemsSource="{Binding Routes}"
                      SelectedItem="{Binding SelectedRoute}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionChanged="RouteDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Route Name" Binding="{Binding RouteName}" Width="120"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <!-- Route Form -->
            <StackPanel Grid.Row="3" Margin="0,10,0,0">
                <TextBlock Text="Route Name:" FontWeight="Bold"/>
                <TextBox Text="{Binding NewRouteName, UpdateSourceTrigger=PropertyChanged}"
                         Height="25" 
                         Margin="0,3,0,8"/>
                
                <TextBlock Text="Description:" FontWeight="Bold"/>
                <TextBox Text="{Binding NewRouteDescription, UpdateSourceTrigger=PropertyChanged}"
                         Height="50" 
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,3,0,10"/>
                
                <Button Content="Add Route" 
                        Command="{Binding AddRouteCommand}"
                        Height="30" 
                        Margin="0,2"
                        Background="LightGreen"/>
                
                <Button Content="Update Route" 
                        Command="{Binding UpdateRouteCommand}"
                        Height="30" 
                        Margin="0,2"
                        Background="LightBlue"/>
                
                <Button Content="Delete Route" 
                        Command="{Binding DeleteRouteCommand}"
                        Height="30" 
                        Margin="0,2"
                        Background="LightCoral"/>
                
                <Button Content="Clear Form" 
                        Command="{Binding ClearFormCommand}"
                        Height="30" 
                        Margin="0,2"/>
            </StackPanel>
        </Grid>
        
        <!-- Route Stations -->
        <Grid Grid.Column="1" Margin="10,5,5,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="Route Stations" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            
            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding RouteStations}"
                      SelectedItem="{Binding SelectedRouteStation}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Seq" Binding="{Binding SequenceNumber}" Width="40"/>
                    <DataGridTextColumn Header="Station" Binding="{Binding Station.StationName}" Width="100"/>
                    <DataGridTextColumn Header="Distance" Binding="{Binding DistanceFromStart}" Width="60"/>
                    <DataGridTextColumn Header="Stop Time" Binding="{Binding DefaultStopTime}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <Button Grid.Row="2" 
                    Content="Remove Station" 
                    Command="{Binding RemoveStationFromRouteCommand}"
                    Height="30" 
                    Margin="0,10,0,0"
                    Background="LightCoral"/>
        </Grid>
        
        <!-- Available Stations -->
        <Grid Grid.Column="2" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="Available Stations" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            
            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding AvailableStations}"
                      SelectedItem="{Binding SelectedAvailableStation}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding StationCode}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding StationName}" Width="120"/>
                    <DataGridTextColumn Header="City" Binding="{Binding City}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
            
            <Button Grid.Row="2" 
                    Content="Add to Route" 
                    Command="{Binding AddStationToRouteCommand}"
                    Height="30" 
                    Margin="0,10,0,0"
                    Background="LightGreen"/>
        </Grid>
    </Grid>
</UserControl>